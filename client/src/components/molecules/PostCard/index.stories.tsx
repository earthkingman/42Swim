import GlobalThemeProvider from '../../../style/GlobalThemeProvider'

export default {
  title: 'Molecules/Modal',
  component: PostCard,
}

export const Default = (props: Props) => {
  return <GlobalThemeProvider></GlobalThemeProvider>
}
